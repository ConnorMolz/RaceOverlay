<internals:Overlay x:Class="RaceOverlay.Overlays.Inputs.Inputs"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:internals="clr-namespace:RaceOverlay.Internals"
        Title="Inputs" Height="{Binding _windowHeight}" Width="{Binding _windowWidth}"
        Background="#FF1E1E1E"
        WindowStyle="None"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize"
        Topmost="True">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid.LayoutTransform>
            <ScaleTransform x:Name="ContentScaleTransform" ScaleX="1" ScaleY="1" />
        </Grid.LayoutTransform>

        <!-- section with speed-->
        <StackPanel Grid.Column="0" 
                    Orientation="Vertical" 
                    Margin="10,0"
                    VerticalAlignment="Bottom">
            <TextBlock Name="SpeedText"
                       Text="278"
                       FontSize="35" 
                       Foreground="White"/>
            <TextBlock Foreground="White" FontSize="15" Text="Km/h"></TextBlock>
            </StackPanel>
        
        <!-- section with input bars -->
        <StackPanel Grid.Column="1" 
                    Orientation="Horizontal" 
                    Margin="10,0"
                    VerticalAlignment="Bottom">
            <!-- Bar 1 (Clutch) -->
            <Grid Height="75">
                <Rectangle Width="10" 
                           Height="75" 
                           Fill="#FF2A2A2A"/>  
                <Rectangle x:Name="ClutchBar"
                           Width="10" 
                           Height="60" 
                           Fill="#FF0000FF"
                           VerticalAlignment="Bottom"/>
            </Grid>
            
            <!-- Bar 2 (Brake)-->
            <Grid Height="75" Margin="5,0">
                <Rectangle Width="10" 
                           Height="75" 
                           Fill="#FF2A2A2A"/>  
                <Rectangle x:Name="BrakeBar"
                           Width="10" 
                           Height="25" 
                           Fill="#FFFF0000"
                           VerticalAlignment="Bottom"/>
            </Grid>
            
            <!-- Bar 3 (Throttle) -->
            <Grid Height="75">
                <Rectangle Width="10" 
                           Height="75" 
                           Fill="#FF2A2A2A"/>  
                <Rectangle x:Name="ThrottleBar"
                           Width="10" 
                           Height="75" 
                           Fill="#FF00FF00"
                           VerticalAlignment="Bottom"/>
            </Grid>
        </StackPanel>

        <!-- gear display -->
        <TextBlock Grid.Column="2" Name="GearText" Text="2" Foreground="White" FontSize="50" TextAlignment="Center" VerticalAlignment="Center"></TextBlock>
        
    </Grid>
</internals:Overlay>